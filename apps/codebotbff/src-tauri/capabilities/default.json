{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:default",
    "shell:execute",
    "shell:open",
    "fs:default",
    "fs:read",
    "fs:write",
    "fs:exists",
    "fs:create",
    "fs:remove",
    "fs:watch"
  ]
}
