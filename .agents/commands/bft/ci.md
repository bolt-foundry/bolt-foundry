---
name: bft-ci
description: Run CI checks (lint, test, build, format). E.g. `bft ci -g` for GH annotations.
---

Run CI checks (lint, test, build, format). E.g. `bft ci -g` for GH annotations.

Execute bft ci command
